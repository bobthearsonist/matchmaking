@model IEnumerable<PlayerMatcher.Game_Sessions>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Session_Start)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Session_End)
        </th>
        <th>
            Game
        </th>
        <th>
            Users
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Session_Start)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Session_End)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Game.Game_Name)
        </td>
        <td>
@foreach (var user in item.Session_Users.ToList())
{
    @Html.DisplayFor(modelItem => user.User.User_Name)  
    @:</br>
}

            
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Game_Session_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Game_Session_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Game_Session_ID })
        </td>
    </tr>
}

</table>
